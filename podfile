platform :ios, '7.0'

xcodeproj 'app.xcodeproj'

pod 'ObjectiveSugar', '~> 0.5'

#target :debug do
	
	#platform :ios, '7.0'

	#pod 'AFNetworking', '~> 2.0'

#end

post_install do |installer|
	
	# can create schemes here for build for each element ...
	installer.project.targets.each do |target|
		
		puts "#{target.name}"		

	end
	
end

